using System.Data;
using TechBooksPublishing.DataAccessLayer;
using TechBooksPublishing.Models;

namespace Lab4
{
    public class Program
    {
        static void Main(string[] args)
        {
           // AuthorDAL d = new AuthorDAL();
           // //d.DeleteAuthorById("100-10-1000");
           //DataTable q =  d.GetAuthors();
           // foreach (DataRow row in q.Rows)
           // {
           //    string name = row["au_fname"].ToString();
           //     Console.WriteLine(name);
           // }

            //d.AddAuthor(new Author() { Id = "100-10-1000",Address=null,FName="test",LName="test",City=null,Contract=false,Phone="01222466368",
            //    State=null,ZipCode=null
            //});

            // q = d.GetAuthors();
            //foreach (DataRow row in q.Rows)
            //{
            //    string name = row["au_fname"].ToString();
            //    Console.WriteLine(name);
            //}
        }
    }
}
