using System.Security.Cryptography;

namespace Lab5ITI;

class Program
{
    static void Main(string[] args)
    {
        // DynamicStack s1 = new DynamicStack(2);
        // DynamicStack s2 = new DynamicStack(2);
        // s1.Push(2);
        // s1.Push(1);
        // s2.Push(4);
        // s2.Push(3);

        // DynamicStack s3 = s2 + s1;
        // s3.Print();

        // DynamicQueue s1 = new DynamicQueue(2);
        // s1.Push(1);
        // s1.Push(2);
        // Console.WriteLine("HEAD point to  " + s1.getHead());
        // s1.Pop();
        // Console.WriteLine("HEAD point to  " + s1.getHead());
        // s1.Pop();
        // Console.WriteLine("HEAD point to  " + s1.getHead());


        // DynamicQueue s1 = new DynamicQueue(2);
        // s1.Push(1);
        // s1.Push(2);
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");
        // s1.Pop();
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");
        // s1.Push(3);
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");

        //Test Full size

        // DynamicQueue s1 = new DynamicQueue(2);
        // s1.Push(1);
        // s1.Push(2);
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");
        // s1.Pop();
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");
        // s1.Push(3);
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");
        // s1.Push(4);
        // Console.WriteLine($"HEAD point to {s1.getHead()} Tail point to {s1.getTail()}");


        // 
        // DynamicQueue s1 = new DynamicQueue(5);
        // s1.Push(1);
        // s1.Push(2);
        // s1.Push(3);
        // s1.Push(4);
        // s1.Pop();
        // s1.Push(6);
    }


}

// dynammic stack
// queue
// complex == , !=

